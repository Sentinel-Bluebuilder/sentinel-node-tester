# Environment & secrets
.env
*.env.local

# Dependencies
node_modules/

# Binaries (V2Ray, WireGuard — downloaded at setup)
bin/v2ray*
bin/v2ray.exe
bin/geoip*.dat
bin/geosite.dat
bin/*.zip
bin/config.json
bin/vpoint_*.json
bin/v2ray-*/
# CLI source IS tracked: bin/cli.js, bin/commands/, bin/lib/

# Runtime data (contains wallet addresses, WG private keys, session credentials)
results/
*.log

# SQLite database (runtime, not for version control)
data/

# Local run-state artifacts (wallet addresses, fee-grant TX hashes, session IDs)
logbuf.json
_currun.json
_lastrun.json
settings-after.json
scripts/research/

# Internal dev files (session handoff, internal suggestions)
HANDOFF.md
suggestions/

# C# bridge build artifacts
csharp-bridge/bin/
csharp-bridge/obj/

# Windows launcher binary
SentinelAudit.exe

# Backup files
*.bak
*.backup
*.local-backup
agent-map.json

# Editor & OS
.vscode/
.idea/
.DS_Store
Thumbs.db
Desktop.ini

# Local test harnesses (not for distribution)
fresh-clone-test/
test-tkd-live.mjs
test-tkd-raw.mjs
