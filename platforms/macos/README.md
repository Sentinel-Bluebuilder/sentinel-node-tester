# macOS Platform Support

Placeholder for future macOS implementation.

Needed:
- WireGuard management via `wg-quick` or NetworkExtension
- V2Ray process management
- VPN interface detection via `ifconfig` / `networksetup`
